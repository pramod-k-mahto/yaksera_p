services:
  backend:
    build: ./backend
    ports:
      - "9000:9000"
    env_file:
      - ./backend/.env
    networks:
      - yaksera_network

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    networks:
      - yaksera_network

networks:
  yaksera_network:
    driver: bridge